[project]
name = "f1-race-manager"
version = "0.1.0"
description = "F1 management race simulation backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.10.0",
    "svgpathtools>=1.7.2",
]

[tool.uv]
package = false
