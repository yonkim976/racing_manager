# Physics V2 completion checklist (temporary)

> 이 문서는 Physics V2 전환 작업을 한 항목씩 완료하기 위한 임시 체크리스트다.
> 모든 항목과 인수 조건이 완료되면 저장소에서 삭제한다.

## 목표

- 레이스를 Physics V2 단일 엔진으로 실행한다.
- 차량 속도가 고정 랩타임/곡률 속도 상한이 아니라 차량에 작용하는 힘과 타이어 그립에서 나오게 한다.
- 레이스 피드, 실제 차량 동작, 그래픽 효과가 동일한 물리 상태를 표현하게 한다.
- 추월을 접근부터 안전한 라인 복귀까지 연속적인 물리 동작으로 처리한다.
- SC/VSC, 피트, 타이어와 교통 효과를 같은 물리 좌표계에서 처리한다.

## 완료 원칙

- 각 단계는 코드, 단위 테스트, 반복 시뮬레이션, 실제 브라우저 검증을 모두 통과해야 완료한다.
- 확률은 행동 자체를 대신하지 않고, 드라이버 의사결정 또는 실제 접촉 이후 결과의 심각도에만 사용한다.
- 이벤트 피드는 물리 상태를 먼저 변경한 뒤 그 상태 전환에서 생성한다.
- 기존 기능을 제거할 때 관련 UI, 명령, 스키마, 테스트와 문서도 함께 정리한다.

## 1. Legacy 엔진 제거

- [x] 개발 패널의 `LEGACY / PHYSICS V2` 선택 UI 제거
- [x] `dev_set_physics_mode` WebSocket 명령 제거
- [x] `RaceEngine.physics_mode` 런타임 선택과 Legacy 진행 분기 제거
- [x] `_distance_based_progress_delta` 등 랩타임 기반 이동 코드 제거
- [x] 모든 엔진 테스트를 Physics V2 기준으로 이전
- [x] 외부 `physics_mode` 필드 제거, `physics_hz=50`만 텔레메트리로 유지
- [x] 서킷/에디터 호환용 기존 좌표 배열은 엔진 Legacy와 구분하여 유지

완료 조건:

- 어떤 실행 경로에서도 Legacy 차량 이동 코드가 호출되지 않는다.
- 개발 UI와 API에서 엔진을 변경할 수 없다.
- 전체 백엔드/프론트엔드 테스트가 통과한다.

상태: **완료** — Backend 158 tests, frontend lint/build 통과.

## 2. 차량 성능 데이터 분해

현재 `Team.car_performance` 한 값을 다음 물리 파라미터로 분해한다.

- [ ] 차량 질량 및 연료 질량 (`mass_kg`, `fuel_kg`)
- [ ] 엔진 최대 출력과 출력 곡선 (`engine_power_kw`, RPM/속도별 가용 출력)
- [ ] 구동계 효율과 트랙션 한계 (`drivetrain_efficiency`, `traction_factor`)
- [ ] 공기저항 면적 (`drag_area_m2`, CdA)
- [ ] 다운포스 면적 (`downforce_area_m2`, ClA)
- [ ] 브레이크 최대 제동력과 페이드 (`brake_force_n`, `brake_fade`)
- [ ] 기계적 그립과 밸런스 (`mechanical_grip`, `aero_balance`)
- [ ] 기존 팀 성능값을 새 파라미터로 마이그레이션
- [ ] 차량별 파라미터 범위와 단위 검증 추가

완료 조건:

- 서로 다른 팀이 직선 속도, 제동, 고속 코너, 저속 코너에서 서로 다른 강약점을 보인다.
- 단일 `car_performance` 배율이 차량의 모든 성능을 동시에 올리지 않는다.

## 3. 힘 기반 종·횡방향 차량 물리

곡률은 속도 상한이 아니라 필요한 횡가속도를 계산하는 입력으로 사용한다.

```text
required_lateral_accel = speed² × curvature
drive_force = min(power × efficiency / speed, traction_limit)
drag_force = 0.5 × air_density × CdA × speed²
downforce = 0.5 × air_density × ClA × speed²
acceleration = (drive_force - drag_force - brake_force - rolling_resistance) / mass
```

- [ ] 현재 `raw_speed_at_progress` 직접 목표속도 방식 제거
- [ ] 운전자 컨트롤러가 코너 진입점에 맞춰 throttle/brake를 결정
- [ ] 출력 제한, 공기저항, 구름저항, 제동력을 50Hz에서 적분
- [ ] 다운포스와 타이어 그립으로 가능한 최대 횡력을 계산
- [ ] 종·횡력이 동시에 필요한 경우 friction circle/ellipse 적용
- [ ] 그립 한계를 넘으면 언더스티어, 오버스티어 또는 런와이드 상태 생성
- [ ] 연료 소모에 따른 질량 변화 적용
- [ ] 결정론적 테스트와 에너지/가속도 범위 검증

완료 조건:

- 직선 최고속도가 출력과 항력의 평형으로 결정된다.
- 코너 속도가 곡률, 다운포스, 기계적 그립과 타이어 상태에서 자연스럽게 결정된다.
- 브레이킹 포인트와 제동거리가 브레이크 성능, 속도, 그립에 따라 달라진다.

## 4. 물리 기반 타이어 모델

- [ ] 컴파운드별 기본 마찰계수와 열 특성 정의
- [ ] 마모가 종방향 트랙션, 제동, 횡그립에 각각 반영되도록 분리
- [ ] 슬립/횡가속/제동 에너지 기반 마모량 계산
- [ ] 타이어 온도 및 적정 온도 구간 모델 추가
- [ ] 과열, 그레이닝, 클리프를 연속적인 그립 변화로 표현
- [ ] 드라이버 타이어 관리 능력은 입력 제어와 슬립 억제에 반영

완료 조건:

- 마모된 타이어는 단순 랩타임 페널티가 아니라 가속, 제동거리와 코너링에서 차이를 만든다.
- 같은 마모도라도 컴파운드와 온도에 따라 다른 거동을 보인다.

## 5. 토잉·더티에어 통합 웨이크 모델

앞차와의 종방향 거리, 횡방향 겹침, 속도와 트랙 곡률로 하나의 `wake_strength`를 계산한다.

- [ ] 앞차 뒤 거리와 좌우 위치로 웨이크 강도 계산
- [ ] 직선/낮은 곡률에서 항력 감소를 적용해 토잉 효과 생성
- [ ] 직선에서 다운포스 손실은 존재하지만 랩타임 영향은 작게 유지
- [ ] 제동과 코너에서 다운포스 손실로 제동 안정성·횡그립 감소
- [ ] 고속 코너일수록 더티에어 영향 증가
- [ ] 횡방향으로 웨이크를 벗어나면 토잉과 더티에어가 점진적으로 감소
- [ ] DRS는 웨이크와 별도로 뒷날개 항력/다운포스를 변경
- [ ] 냉각 저하와 타이어 과열은 후속 단계로 추가
- [ ] 기존 `dirty_air_active` 불리언을 연속 값 텔레메트리로 교체

예상 동작:

```text
직선: 앞차 뒤에 붙음 → drag 감소 → 가속/최고속도 증가 (토잉)
제동: downforce 감소 → 안정성 및 최대 제동력 일부 감소
코너: downforce 감소 → 횡그립 감소 → 더 느린 진입 또는 언더스티어
라인 이탈: 웨이크 감소 → 깨끗한 공기와 그립 회복
```

완료 조건:

- 같은 간격에서도 직선에서는 이득, 고속 코너에서는 손실이 발생한다.
- 토잉을 받다가 추월 라인으로 이동하면 항력 이득이 자연스럽게 줄어든다.
- 효과가 고정 랩타임 보너스/페널티를 사용하지 않는다.

## 6. 이벤트 피드와 물리/그래픽 동기화

- [ ] `ActiveManeuver` 또는 동등한 물리 행동 상태 추가
- [ ] 이벤트를 확률 결과가 아니라 물리 상태 전환에서 생성
- [ ] `attack`, `defend`, `lockup`, `run_wide`, `traction_loss`, `contact`, `pass` 단계 정의
- [ ] 각 행동의 시작 시각, 차량, 트랙 위치, 목표 라인과 결과를 텔레메트리에 포함
- [ ] 락업 연기, 스파크, 흔들림, 런와이드 등 Canvas 표현 추가
- [ ] 속도 및 시간 격차가 실제 물리 행동의 결과로 발생하도록 변경
- [ ] 피드 문장과 그래픽 시작 프레임의 시각 차이 제한

완료 조건:

- 피드에 표시되는 모든 주요 행동을 같은 시점에 화면에서도 확인할 수 있다.
- 화면에 없는 접촉/런와이드가 피드에만 표시되지 않는다.

## 7. 추월 상태 머신과 실제 접촉

- [ ] `APPROACH → PULL_OUT → OVERLAP → CLEAR → MERGE → COMPLETE/ABORT` 구현
- [ ] 차폭이 확보되기 전에는 앞차 추종 제어 유지
- [ ] 공격차와 방어차가 서로의 점유 공간과 목표 라인을 고려
- [ ] 공격차 뒤 범퍼가 완전히 앞선 뒤에만 `CLEAR` 판정
- [ ] 안전 간격을 확보한 뒤 레이싱 라인으로 복귀
- [ ] 실패한 추월은 순간이동 없이 자연스럽게 뒤로 복귀
- [ ] 사후 5m 위치 스냅을 비상 안전장치로 축소 또는 제거
- [ ] 실제 차체 영역과 상대 접근속도로 접촉 판정
- [ ] 접촉 이후 결과의 심각도에만 확률 사용
- [ ] 서킷 폭, 코너 방향, 라인별 그립으로 추월 난이도 계산

완료 조건:

- 정상 추월에서 차량이 겹치거나 5m 뒤로 순간이동하지 않는다.
- 실제 차체가 닿은 경우에만 접촉 그래픽과 이벤트가 발생한다.
- 서킷별 목표 추월 빈도와 성공률 범위가 반복 시뮬레이션에서 유지된다.

## 8. 고배율 렌더링과 텔레메트리

- [ ] 5Hz 위치 스냅샷 대신 timestamp 기반 버퍼 보간 적용
- [ ] 1500/2000%에서 차량과 카메라가 같은 보간 상태를 사용
- [ ] 가속도, 횡속도, yaw/heading을 클라이언트 예측에 포함
- [ ] 2x/5x에서 focused driver 텔레메트리 전송률 상향 검토
- [ ] 차량 Graphics를 매 스냅샷마다 재생성하지 않도록 캐시
- [ ] 장시간 주행 프레임타임 및 메모리 계측 추가

완료 조건:

- 1x/2x/5x에서 장시간 고배율 주행 시 주기적 위치 보정이 눈에 띄지 않는다.
- 30분 이상 실행에서 프레임타임과 메모리가 지속적으로 악화되지 않는다.

## 9. 피트·SC/VSC 물리 통합

- [ ] 피트 진입/제한속도/박스 정차/피트 아웃을 거리 기반 물리로 이동
- [ ] SC 차량도 동일한 거리·속도·차체 모델 사용
- [ ] SC/VSC 델타를 랩타임 배율이 아닌 속도 목표와 제어 규칙으로 변경
- [ ] 피트와 트랙 차량의 순위를 연속 거리로 계산
- [ ] SC 중 피트, 언랩, 재시작 회귀 테스트 유지

## 10. 튜닝과 최종 검증

- [ ] 팀별 직선·저속·고속·제동 성능 벤치마크
- [ ] 서킷별 최고속도, 코너 최저속도, 랩타임 범위 검증
- [ ] 타이어 컴파운드 및 스틴트 길이 검증
- [ ] 토잉/더티에어 거리별 효과 곡선 검증
- [ ] 추월 시도·성공·접촉 빈도 반복 시뮬레이션
- [ ] SC/VSC/피트/일시정지 전체 회귀 테스트
- [ ] 프론트엔드 lint/build 및 실제 브라우저 장시간 검증
- [ ] 기존 설계 문서와 중복된 완료 내역 정리
- [ ] 모든 항목 완료 후 이 임시 체크리스트 삭제

## 권장 구현 순서

1. Legacy 엔진 제거
2. 차량 성능 데이터 분해
3. 힘 기반 종·횡방향 물리
4. 타이어 물리
5. 토잉·더티에어 웨이크
6. 이벤트/그래픽 동기화
7. 추월 및 실제 접촉
8. 고배율 보간
9. 피트·SC/VSC 통합
10. 전체 튜닝 및 문서 삭제
